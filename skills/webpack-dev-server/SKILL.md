---
name: "webpack-dev-server"
version: "5.2.3"
downloads: 67.4M/month
description: >
  Serves a webpack app. Updates the browser on changes.. Use when: building REST API servers; middleware-based request handling; route management. NOT for: client-side browser applications; static file serving without a server.
---

# webpack-dev-server

## Overview
Serves a webpack app. Updates the browser on changes.. This should be used for development only.

## Installation
```bash
npm install webpack-dev-server
```

## Core API / Usage
```js
_Note: While you can install and run webpack-dev-server globally, we recommend
installing it locally. webpack-dev-server will always use a local installation
over a global one._

## Usage

There are two main, recommended methods of using the module:

### With the CLI

The easiest way to use it is with the [webpack CLI](https://webpack.js.org/api/cli/). In the directory where your
`webpack.config.js` is, run:
```

```js
Following options are available with `webpack serve`:
```

## Common Patterns
### Key Features

- **Getting Started**
- **With the CLI**
- **With the API**
- **With TypeScript**

### Example

```js
Following options are available with `webpack serve`:
```

## Configuration
```js
_Note: While you can install and run webpack-dev-server globally, we recommend
installing it locally. webpack-dev-server will always use a local installation
over a global one._

## Usage

There are two main, recommended methods of using the module:

### With the CLI

The easiest way to use it is with the [webpack CLI](https://webpack.js.org/api/cli/). In the directory where your
`webpack.config.js` is, run:
```

## Tips & Gotchas
- Includes built-in TypeScript type definitions.
- Works in both Node.js and browser environments.
