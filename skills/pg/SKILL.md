---
name: "pg"
version: "8.19.0"
downloads: 77.6M/month
description: >
  PostgreSQL client - pure javascript & libpq with the same API. Use when: Pure JavaScript client and native libpq bindings share _the same API_; Connection pooling; Extensible JS ↔ PostgreSQL data-type coercion. NOT for: web browser UI rendering; server-side HTML generation.
---

# pg

## Overview
PostgreSQL client - pure javascript & libpq with the same API. Pure JavaScript and optional native libpq bindings.

## Installation
```bash
npm install pg
```

## Core API / Usage
```js
$ npm install pg
```

## Common Patterns
### Key Features

- **Pure JavaScript client and native libpq bindings share _the same API_**
- **Connection pooling**
- **Extensible JS ↔ PostgreSQL data-type coercion**
- **Supported PostgreSQL features**

## Configuration
See the [official documentation](https://www.npmjs.com/package/pg) for configuration options and advanced settings.

## Tips & Gotchas
- Current version: 8.19.0. Check the changelog when upgrading across major versions.
- Refer to the official npm page for edge cases and advanced configuration.
