---
name: "ws"
version: "8.19.0"
downloads: 658.9M/month
description: >
  Simple to use, blazing fast and thoroughly tested websocket client and server for Node.js. Use when: Protocol support; Opt-in for performance; Legacy opt-in for performance. NOT for: production runtime logic; replacing static type checking.
---

# ws

## Overview
Simple to use, blazing fast and thoroughly tested websocket client and server for Node.js. # ws: a Node.js WebSocket library ws is a simple to use, blazing fast, and thoroughly tested WebSocket client and server implementation.

## Installation
```bash
npm install ws
```

## Core API / Usage
```bash
npm install --save-optional bufferutil
```

```bash
npm install --save-optional utf-8-validate
```

## Common Patterns
### Key Features

- **Protocol support**
- **Opt-in for performance**
- **Legacy opt-in for performance**
- **WebSocket compression**

### Example

```bash
npm install --save-optional utf-8-validate
```

## Configuration
```bash
npm install --save-optional bufferutil
```

## Tips & Gotchas
- This package is Node.js only — it does not work in browser environments.
- Current version: 8.19.0. Check the changelog when upgrading across major versions.
